import DaveyThesis2024.Delta4.Generated.L8Masks
import DaveyThesis2024.Delta4.Generated.L8H00
import DaveyThesis2024.Delta4.Generated.L8H01
import DaveyThesis2024.Delta4.Generated.L8H02
import DaveyThesis2024.Delta4.Generated.L8H03
import DaveyThesis2024.Delta4.Generated.L8S00

/-
  AUTO-GENERATED by DaveyThesis2024/Delta4/Generated/generator/gen_layer.py.
  DO NOT EDIT -- regenerate instead.
  Layer 8: the assembly — 172 masks, 282124 nodes, 0 violations.
  Node counts cross-checked by two independent implementations
  (model_a.py, incremental; model_b.c, recomputed from scratch).
-/

namespace Delta4Gen.L8
open Delta4Model Delta4Chunk

-- The mask-list lookup for index 11.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K011 in
theorem M_at_11 : (M.drop 11).take 1 = [K011] := by
  decide +kernel

/-- **The last hop** for mask 11: the per-mask node count becomes the
    one-mask slice `[11, 12)` of the layer loop. -/
theorem slice_1 : msCnt 8 ((M.drop 11).take 1) = (12884, 0) := by
  rw [M_at_11, msCnt_singleton]
  exact mask011_count

-- The mask-list lookup for index 23.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K023 in
theorem M_at_23 : (M.drop 23).take 1 = [K023] := by
  decide +kernel

/-- **The last hop** for mask 23: the per-mask node count becomes the
    one-mask slice `[23, 24)` of the layer loop. -/
theorem slice_4 : msCnt 8 ((M.drop 23).take 1) = (4019, 0) := by
  rw [M_at_23, msCnt_singleton]
  exact mask023_count

-- The mask-list lookup for index 24.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K024 in
theorem M_at_24 : (M.drop 24).take 1 = [K024] := by
  decide +kernel

/-- **The last hop** for mask 24: the per-mask node count becomes the
    one-mask slice `[24, 25)` of the layer loop. -/
theorem slice_5 : msCnt 8 ((M.drop 24).take 1) = (5362, 0) := by
  rw [M_at_24, msCnt_singleton]
  exact mask024_count

-- The mask-list lookup for index 42.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K042 in
theorem M_at_42 : (M.drop 42).take 1 = [K042] := by
  decide +kernel

/-- **The last hop** for mask 42: the per-mask node count becomes the
    one-mask slice `[42, 43)` of the layer loop. -/
theorem slice_8 : msCnt 8 ((M.drop 42).take 1) = (3211, 0) := by
  rw [M_at_42, msCnt_singleton]
  exact mask042_count

-- The mask-list lookup for index 50.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K050 in
theorem M_at_50 : (M.drop 50).take 1 = [K050] := by
  decide +kernel

/-- **The last hop** for mask 50: the per-mask node count becomes the
    one-mask slice `[50, 51)` of the layer loop. -/
theorem slice_12 : msCnt 8 ((M.drop 50).take 1) = (12884, 0) := by
  rw [M_at_50, msCnt_singleton]
  exact mask050_count

-- The mask-list lookup for index 54.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K054 in
theorem M_at_54 : (M.drop 54).take 1 = [K054] := by
  decide +kernel

/-- **The last hop** for mask 54: the per-mask node count becomes the
    one-mask slice `[54, 55)` of the layer loop. -/
theorem slice_14 : msCnt 8 ((M.drop 54).take 1) = (5362, 0) := by
  rw [M_at_54, msCnt_singleton]
  exact mask054_count

-- The mask-list lookup for index 56.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K056 in
theorem M_at_56 : (M.drop 56).take 1 = [K056] := by
  decide +kernel

/-- **The last hop** for mask 56: the per-mask node count becomes the
    one-mask slice `[56, 57)` of the layer loop. -/
theorem slice_16 : msCnt 8 ((M.drop 56).take 1) = (3211, 0) := by
  rw [M_at_56, msCnt_singleton]
  exact mask056_count

-- The mask-list lookup for index 59.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K059 in
theorem M_at_59 : (M.drop 59).take 1 = [K059] := by
  decide +kernel

/-- **The last hop** for mask 59: the per-mask node count becomes the
    one-mask slice `[59, 60)` of the layer loop. -/
theorem slice_18 : msCnt 8 ((M.drop 59).take 1) = (16320, 0) := by
  rw [M_at_59, msCnt_singleton]
  exact mask059_count

-- The mask-list lookup for index 66.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K066 in
theorem M_at_66 : (M.drop 66).take 1 = [K066] := by
  decide +kernel

/-- **The last hop** for mask 66: the per-mask node count becomes the
    one-mask slice `[66, 67)` of the layer loop. -/
theorem slice_20 : msCnt 8 ((M.drop 66).take 1) = (3748, 0) := by
  rw [M_at_66, msCnt_singleton]
  exact mask066_count

-- The mask-list lookup for index 67.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K067 in
theorem M_at_67 : (M.drop 67).take 1 = [K067] := by
  decide +kernel

/-- **The last hop** for mask 67: the per-mask node count becomes the
    one-mask slice `[67, 68)` of the layer loop. -/
theorem slice_21 : msCnt 8 ((M.drop 67).take 1) = (5621, 0) := by
  rw [M_at_67, msCnt_singleton]
  exact mask067_count

-- The mask-list lookup for index 83.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K083 in
theorem M_at_83 : (M.drop 83).take 1 = [K083] := by
  decide +kernel

/-- **The last hop** for mask 83: the per-mask node count becomes the
    one-mask slice `[83, 84)` of the layer loop. -/
theorem slice_24 : msCnt 8 ((M.drop 83).take 1) = (3357, 0) := by
  rw [M_at_83, msCnt_singleton]
  exact mask083_count

-- The mask-list lookup for index 87.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K087 in
theorem M_at_87 : (M.drop 87).take 1 = [K087] := by
  decide +kernel

/-- **The last hop** for mask 87: the per-mask node count becomes the
    one-mask slice `[87, 88)` of the layer loop. -/
theorem slice_26 : msCnt 8 ((M.drop 87).take 1) = (19689, 0) := by
  rw [M_at_87, msCnt_singleton]
  exact mask087_count

-- The mask-list lookup for index 94.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K094 in
theorem M_at_94 : (M.drop 94).take 1 = [K094] := by
  decide +kernel

/-- **The last hop** for mask 94: the per-mask node count becomes the
    one-mask slice `[94, 95)` of the layer loop. -/
theorem slice_28 : msCnt 8 ((M.drop 94).take 1) = (3199, 0) := by
  rw [M_at_94, msCnt_singleton]
  exact mask094_count

-- The mask-list lookup for index 96.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K096 in
theorem M_at_96 : (M.drop 96).take 1 = [K096] := by
  decide +kernel

/-- **The last hop** for mask 96: the per-mask node count becomes the
    one-mask slice `[96, 97)` of the layer loop. -/
theorem slice_30 : msCnt 8 ((M.drop 96).take 1) = (3121, 0) := by
  rw [M_at_96, msCnt_singleton]
  exact mask096_count

-- The mask-list lookup for index 107.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K107 in
theorem M_at_107 : (M.drop 107).take 1 = [K107] := by
  decide +kernel

/-- **The last hop** for mask 107: the per-mask node count becomes the
    one-mask slice `[107, 108)` of the layer loop. -/
theorem slice_34 : msCnt 8 ((M.drop 107).take 1) = (10732, 0) := by
  rw [M_at_107, msCnt_singleton]
  exact mask107_count

-- The mask-list lookup for index 111.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K111 in
theorem M_at_111 : (M.drop 111).take 1 = [K111] := by
  decide +kernel

/-- **The last hop** for mask 111: the per-mask node count becomes the
    one-mask slice `[111, 112)` of the layer loop. -/
theorem slice_36 : msCnt 8 ((M.drop 111).take 1) = (10174, 0) := by
  rw [M_at_111, msCnt_singleton]
  exact mask111_count

-- The mask-list lookup for index 114.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K114 in
theorem M_at_114 : (M.drop 114).take 1 = [K114] := by
  decide +kernel

/-- **The last hop** for mask 114: the per-mask node count becomes the
    one-mask slice `[114, 115)` of the layer loop. -/
theorem slice_38 : msCnt 8 ((M.drop 114).take 1) = (5509, 0) := by
  rw [M_at_114, msCnt_singleton]
  exact mask114_count

-- The mask-list lookup for index 115.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K115 in
theorem M_at_115 : (M.drop 115).take 1 = [K115] := by
  decide +kernel

/-- **The last hop** for mask 115: the per-mask node count becomes the
    one-mask slice `[115, 116)` of the layer loop. -/
theorem slice_39 : msCnt 8 ((M.drop 115).take 1) = (6308, 0) := by
  rw [M_at_115, msCnt_singleton]
  exact mask115_count

-- The mask-list lookup for index 118.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K118 in
theorem M_at_118 : (M.drop 118).take 1 = [K118] := by
  decide +kernel

/-- **The last hop** for mask 118: the per-mask node count becomes the
    one-mask slice `[118, 119)` of the layer loop. -/
theorem slice_41 : msCnt 8 ((M.drop 118).take 1) = (3411, 0) := by
  rw [M_at_118, msCnt_singleton]
  exact mask118_count

-- The mask-list lookup for index 119.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K119 in
theorem M_at_119 : (M.drop 119).take 1 = [K119] := by
  decide +kernel

/-- **The last hop** for mask 119: the per-mask node count becomes the
    one-mask slice `[119, 120)` of the layer loop. -/
theorem slice_42 : msCnt 8 ((M.drop 119).take 1) = (5774, 0) := by
  rw [M_at_119, msCnt_singleton]
  exact mask119_count

-- The mask-list lookup for index 122.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K122 in
theorem M_at_122 : (M.drop 122).take 1 = [K122] := by
  decide +kernel

/-- **The last hop** for mask 122: the per-mask node count becomes the
    one-mask slice `[122, 123)` of the layer loop. -/
theorem slice_44 : msCnt 8 ((M.drop 122).take 1) = (5421, 0) := by
  rw [M_at_122, msCnt_singleton]
  exact mask122_count

-- The mask-list lookup for index 127.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K127 in
theorem M_at_127 : (M.drop 127).take 1 = [K127] := by
  decide +kernel

/-- **The last hop** for mask 127: the per-mask node count becomes the
    one-mask slice `[127, 128)` of the layer loop. -/
theorem slice_47 : msCnt 8 ((M.drop 127).take 1) = (6512, 0) := by
  rw [M_at_127, msCnt_singleton]
  exact mask127_count

-- The mask-list lookup for index 143.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K143 in
theorem M_at_143 : (M.drop 143).take 1 = [K143] := by
  decide +kernel

/-- **The last hop** for mask 143: the per-mask node count becomes the
    one-mask slice `[143, 144)` of the layer loop. -/
theorem slice_52 : msCnt 8 ((M.drop 143).take 1) = (12884, 0) := by
  rw [M_at_143, msCnt_singleton]
  exact mask143_count

-- The mask-list lookup for index 146.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K146 in
theorem M_at_146 : (M.drop 146).take 1 = [K146] := by
  decide +kernel

/-- **The last hop** for mask 146: the per-mask node count becomes the
    one-mask slice `[146, 147)` of the layer loop. -/
theorem slice_54 : msCnt 8 ((M.drop 146).take 1) = (3199, 0) := by
  rw [M_at_146, msCnt_singleton]
  exact mask146_count

-- The mask-list lookup for index 147.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K147 in
theorem M_at_147 : (M.drop 147).take 1 = [K147] := by
  decide +kernel

/-- **The last hop** for mask 147: the per-mask node count becomes the
    one-mask slice `[147, 148)` of the layer loop. -/
theorem slice_55 : msCnt 8 ((M.drop 147).take 1) = (5104, 0) := by
  rw [M_at_147, msCnt_singleton]
  exact mask147_count

-- The mask-list lookup for index 149.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K149 in
theorem M_at_149 : (M.drop 149).take 1 = [K149] := by
  decide +kernel

/-- **The last hop** for mask 149: the per-mask node count becomes the
    one-mask slice `[149, 150)` of the layer loop. -/
theorem slice_57 : msCnt 8 ((M.drop 149).take 1) = (16320, 0) := by
  rw [M_at_149, msCnt_singleton]
  exact mask149_count

-- The mask-list lookup for index 153.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K153 in
theorem M_at_153 : (M.drop 153).take 1 = [K153] := by
  decide +kernel

/-- **The last hop** for mask 153: the per-mask node count becomes the
    one-mask slice `[153, 154)` of the layer loop. -/
theorem slice_59 : msCnt 8 ((M.drop 153).take 1) = (3411, 0) := by
  rw [M_at_153, msCnt_singleton]
  exact mask153_count

-- The mask-list lookup for index 154.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K154 in
theorem M_at_154 : (M.drop 154).take 1 = [K154] := by
  decide +kernel

/-- **The last hop** for mask 154: the per-mask node count becomes the
    one-mask slice `[154, 155)` of the layer loop. -/
theorem slice_60 : msCnt 8 ((M.drop 154).take 1) = (5774, 0) := by
  rw [M_at_154, msCnt_singleton]
  exact mask154_count

-- The mask-list lookup for index 159.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K159 in
theorem M_at_159 : (M.drop 159).take 1 = [K159] := by
  decide +kernel

/-- **The last hop** for mask 159: the per-mask node count becomes the
    one-mask slice `[159, 160)` of the layer loop. -/
theorem slice_62 : msCnt 8 ((M.drop 159).take 1) = (5421, 0) := by
  rw [M_at_159, msCnt_singleton]
  exact mask159_count

-- The mask-list lookup for index 161.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K161 in
theorem M_at_161 : (M.drop 161).take 1 = [K161] := by
  decide +kernel

/-- **The last hop** for mask 161: the per-mask node count becomes the
    one-mask slice `[161, 162)` of the layer loop. -/
theorem slice_64 : msCnt 8 ((M.drop 161).take 1) = (6512, 0) := by
  rw [M_at_161, msCnt_singleton]
  exact mask161_count

-- The mask-list lookup for index 165.  `unseal` is kept because `decide` can
-- ask the *elaborator* to reduce the `Decidable` instance, and there
-- `@[irreducible]` bites (Layer7.lean, "THE SEAM").  A doc comment cannot
-- precede `unseal ... in`, hence the line comments.
unseal M K165 in
theorem M_at_165 : (M.drop 165).take 1 = [K165] := by
  decide +kernel

/-- **The last hop** for mask 165: the per-mask node count becomes the
    one-mask slice `[165, 166)` of the layer loop. -/
theorem slice_66 : msCnt 8 ((M.drop 165).take 1) = (14069, 0) := by
  rw [M_at_165, msCnt_singleton]
  exact mask165_count

/-! ## The layer, chained.  69 slices, offsets running 0 → 172 with no gap:
    a dropped or misordered slice fails to typecheck at the seam. -/

theorem m_69 : msCnt 8 (M.drop 172) = (0, 0) := msCnt_nil M_residual

theorem m_68 : msCnt 8 (M.drop 169) = (1976, 0) :=
  stepM (b := 3) (c := 172) rfl slice_68 m_69 rfl

theorem m_67 : msCnt 8 (M.drop 166) = (3952, 0) :=
  stepM (b := 3) (c := 169) rfl slice_67 m_68 rfl

theorem m_66 : msCnt 8 (M.drop 165) = (18021, 0) :=
  stepM (b := 1) (c := 166) rfl slice_66 m_67 rfl

theorem m_65 : msCnt 8 (M.drop 162) = (19986, 0) :=
  stepM (b := 3) (c := 165) rfl slice_65 m_66 rfl

theorem m_64 : msCnt 8 (M.drop 161) = (26498, 0) :=
  stepM (b := 1) (c := 162) rfl slice_64 m_65 rfl

theorem m_63 : msCnt 8 (M.drop 160) = (26499, 0) :=
  stepM (b := 1) (c := 161) rfl slice_63 m_64 rfl

theorem m_62 : msCnt 8 (M.drop 159) = (31920, 0) :=
  stepM (b := 1) (c := 160) rfl slice_62 m_63 rfl

theorem m_61 : msCnt 8 (M.drop 155) = (33063, 0) :=
  stepM (b := 4) (c := 159) rfl slice_61 m_62 rfl

theorem m_60 : msCnt 8 (M.drop 154) = (38837, 0) :=
  stepM (b := 1) (c := 155) rfl slice_60 m_61 rfl

theorem m_59 : msCnt 8 (M.drop 153) = (42248, 0) :=
  stepM (b := 1) (c := 154) rfl slice_59 m_60 rfl

theorem m_58 : msCnt 8 (M.drop 150) = (44354, 0) :=
  stepM (b := 3) (c := 153) rfl slice_58 m_59 rfl

theorem m_57 : msCnt 8 (M.drop 149) = (60674, 0) :=
  stepM (b := 1) (c := 150) rfl slice_57 m_58 rfl

theorem m_56 : msCnt 8 (M.drop 148) = (60675, 0) :=
  stepM (b := 1) (c := 149) rfl slice_56 m_57 rfl

theorem m_55 : msCnt 8 (M.drop 147) = (65779, 0) :=
  stepM (b := 1) (c := 148) rfl slice_55 m_56 rfl

theorem m_54 : msCnt 8 (M.drop 146) = (68978, 0) :=
  stepM (b := 1) (c := 147) rfl slice_54 m_55 rfl

theorem m_53 : msCnt 8 (M.drop 144) = (68980, 0) :=
  stepM (b := 2) (c := 146) rfl slice_53 m_54 rfl

theorem m_52 : msCnt 8 (M.drop 143) = (81864, 0) :=
  stepM (b := 1) (c := 144) rfl slice_52 m_53 rfl

theorem m_51 : msCnt 8 (M.drop 142) = (83919, 0) :=
  stepM (b := 1) (c := 143) rfl slice_51 m_52 rfl

theorem m_50 : msCnt 8 (M.drop 139) = (86145, 0) :=
  stepM (b := 3) (c := 142) rfl slice_50 m_51 rfl

theorem m_49 : msCnt 8 (M.drop 138) = (87203, 0) :=
  stepM (b := 1) (c := 139) rfl slice_49 m_50 rfl

theorem m_48 : msCnt 8 (M.drop 128) = (89318, 0) :=
  stepM (b := 10) (c := 138) rfl slice_48 m_49 rfl

theorem m_47 : msCnt 8 (M.drop 127) = (95830, 0) :=
  stepM (b := 1) (c := 128) rfl slice_47 m_48 rfl

theorem m_46 : msCnt 8 (M.drop 126) = (97756, 0) :=
  stepM (b := 1) (c := 127) rfl slice_46 m_47 rfl

theorem m_45 : msCnt 8 (M.drop 123) = (98907, 0) :=
  stepM (b := 3) (c := 126) rfl slice_45 m_46 rfl

theorem m_44 : msCnt 8 (M.drop 122) = (104328, 0) :=
  stepM (b := 1) (c := 123) rfl slice_44 m_45 rfl

theorem m_43 : msCnt 8 (M.drop 120) = (104330, 0) :=
  stepM (b := 2) (c := 122) rfl slice_43 m_44 rfl

theorem m_42 : msCnt 8 (M.drop 119) = (110104, 0) :=
  stepM (b := 1) (c := 120) rfl slice_42 m_43 rfl

theorem m_41 : msCnt 8 (M.drop 118) = (113515, 0) :=
  stepM (b := 1) (c := 119) rfl slice_41 m_42 rfl

theorem m_40 : msCnt 8 (M.drop 116) = (113517, 0) :=
  stepM (b := 2) (c := 118) rfl slice_40 m_41 rfl

theorem m_39 : msCnt 8 (M.drop 115) = (119825, 0) :=
  stepM (b := 1) (c := 116) rfl slice_39 m_40 rfl

theorem m_38 : msCnt 8 (M.drop 114) = (125334, 0) :=
  stepM (b := 1) (c := 115) rfl slice_38 m_39 rfl

theorem m_37 : msCnt 8 (M.drop 112) = (125336, 0) :=
  stepM (b := 2) (c := 114) rfl slice_37 m_38 rfl

theorem m_36 : msCnt 8 (M.drop 111) = (135510, 0) :=
  stepM (b := 1) (c := 112) rfl slice_36 m_37 rfl

theorem m_35 : msCnt 8 (M.drop 108) = (137965, 0) :=
  stepM (b := 3) (c := 111) rfl slice_35 m_36 rfl

theorem m_34 : msCnt 8 (M.drop 107) = (148697, 0) :=
  stepM (b := 1) (c := 108) rfl slice_34 m_35 rfl

theorem m_33 : msCnt 8 (M.drop 104) = (150939, 0) :=
  stepM (b := 3) (c := 107) rfl slice_33 m_34 rfl

theorem m_32 : msCnt 8 (M.drop 102) = (153366, 0) :=
  stepM (b := 2) (c := 104) rfl slice_32 m_33 rfl

theorem m_31 : msCnt 8 (M.drop 97) = (155299, 0) :=
  stepM (b := 5) (c := 102) rfl slice_31 m_32 rfl

theorem m_30 : msCnt 8 (M.drop 96) = (158420, 0) :=
  stepM (b := 1) (c := 97) rfl slice_30 m_31 rfl

theorem m_29 : msCnt 8 (M.drop 95) = (158421, 0) :=
  stepM (b := 1) (c := 96) rfl slice_29 m_30 rfl

theorem m_28 : msCnt 8 (M.drop 94) = (161620, 0) :=
  stepM (b := 1) (c := 95) rfl slice_28 m_29 rfl

theorem m_27 : msCnt 8 (M.drop 88) = (162395, 0) :=
  stepM (b := 6) (c := 94) rfl slice_27 m_28 rfl

theorem m_26 : msCnt 8 (M.drop 87) = (182084, 0) :=
  stepM (b := 1) (c := 88) rfl slice_26 m_27 rfl

theorem m_25 : msCnt 8 (M.drop 84) = (183267, 0) :=
  stepM (b := 3) (c := 87) rfl slice_25 m_26 rfl

theorem m_24 : msCnt 8 (M.drop 83) = (186624, 0) :=
  stepM (b := 1) (c := 84) rfl slice_24 m_25 rfl

theorem m_23 : msCnt 8 (M.drop 79) = (189185, 0) :=
  stepM (b := 4) (c := 83) rfl slice_23 m_24 rfl

theorem m_22 : msCnt 8 (M.drop 68) = (189968, 0) :=
  stepM (b := 11) (c := 79) rfl slice_22 m_23 rfl

theorem m_21 : msCnt 8 (M.drop 67) = (195589, 0) :=
  stepM (b := 1) (c := 68) rfl slice_21 m_22 rfl

theorem m_20 : msCnt 8 (M.drop 66) = (199337, 0) :=
  stepM (b := 1) (c := 67) rfl slice_20 m_21 rfl

theorem m_19 : msCnt 8 (M.drop 60) = (199343, 0) :=
  stepM (b := 6) (c := 66) rfl slice_19 m_20 rfl

theorem m_18 : msCnt 8 (M.drop 59) = (215663, 0) :=
  stepM (b := 1) (c := 60) rfl slice_18 m_19 rfl

theorem m_17 : msCnt 8 (M.drop 57) = (217888, 0) :=
  stepM (b := 2) (c := 59) rfl slice_17 m_18 rfl

theorem m_16 : msCnt 8 (M.drop 56) = (221099, 0) :=
  stepM (b := 1) (c := 57) rfl slice_16 m_17 rfl

theorem m_15 : msCnt 8 (M.drop 55) = (221100, 0) :=
  stepM (b := 1) (c := 56) rfl slice_15 m_16 rfl

theorem m_14 : msCnt 8 (M.drop 54) = (226462, 0) :=
  stepM (b := 1) (c := 55) rfl slice_14 m_15 rfl

theorem m_13 : msCnt 8 (M.drop 51) = (228803, 0) :=
  stepM (b := 3) (c := 54) rfl slice_13 m_14 rfl

theorem m_12 : msCnt 8 (M.drop 50) = (241687, 0) :=
  stepM (b := 1) (c := 51) rfl slice_12 m_13 rfl

theorem m_11 : msCnt 8 (M.drop 48) = (243912, 0) :=
  stepM (b := 2) (c := 50) rfl slice_11 m_12 rfl

theorem m_10 : msCnt 8 (M.drop 47) = (244970, 0) :=
  stepM (b := 1) (c := 48) rfl slice_10 m_11 rfl

theorem m_9 : msCnt 8 (M.drop 43) = (247079, 0) :=
  stepM (b := 4) (c := 47) rfl slice_9 m_10 rfl

theorem m_8 : msCnt 8 (M.drop 42) = (250290, 0) :=
  stepM (b := 1) (c := 43) rfl slice_8 m_9 rfl

theorem m_7 : msCnt 8 (M.drop 36) = (252188, 0) :=
  stepM (b := 6) (c := 42) rfl slice_7 m_8 rfl

theorem m_6 : msCnt 8 (M.drop 25) = (255017, 0) :=
  stepM (b := 11) (c := 36) rfl slice_6 m_7 rfl

theorem m_5 : msCnt 8 (M.drop 24) = (260379, 0) :=
  stepM (b := 1) (c := 25) rfl slice_5 m_6 rfl

theorem m_4 : msCnt 8 (M.drop 23) = (264398, 0) :=
  stepM (b := 1) (c := 24) rfl slice_4 m_5 rfl

theorem m_3 : msCnt 8 (M.drop 20) = (266739, 0) :=
  stepM (b := 3) (c := 23) rfl slice_3 m_4 rfl

theorem m_2 : msCnt 8 (M.drop 12) = (269229, 0) :=
  stepM (b := 8) (c := 20) rfl slice_2 m_3 rfl

theorem m_1 : msCnt 8 (M.drop 11) = (282113, 0) :=
  stepM (b := 1) (c := 12) rfl slice_1 m_2 rfl

theorem m_0 : msCnt 8 (M.drop 0) = (282124, 0) :=
  stepM (b := 11) (c := 11) rfl slice_0 m_1 rfl

theorem layer_count : msCnt 8 M = (282124, 0) := by
  have h := m_0
  rwa [List.drop_zero] at h

/-- **Layer 8 of `checkAll`.**  What `AssemblyLayers.checkAll_of_layers`
    consumes — carried by a node count, which is the only thing that
    distinguishes one layer's proof from another's. -/
theorem layer_ok : Delta4Assembly.LayerOK 8 := by
  refine layerOK_of_msCnt (N := 282124) ?_
  rw [M_eq]
  exact layer_count

end Delta4Gen.L8

#print axioms Delta4Gen.L8.M_at_11
#print axioms Delta4Gen.L8.slice_1
#print axioms Delta4Gen.L8.M_at_23
#print axioms Delta4Gen.L8.slice_4
#print axioms Delta4Gen.L8.M_at_24
#print axioms Delta4Gen.L8.slice_5
#print axioms Delta4Gen.L8.M_at_42
#print axioms Delta4Gen.L8.slice_8
#print axioms Delta4Gen.L8.M_at_50
#print axioms Delta4Gen.L8.slice_12
#print axioms Delta4Gen.L8.M_at_54
#print axioms Delta4Gen.L8.slice_14
#print axioms Delta4Gen.L8.M_at_56
#print axioms Delta4Gen.L8.slice_16
#print axioms Delta4Gen.L8.M_at_59
#print axioms Delta4Gen.L8.slice_18
#print axioms Delta4Gen.L8.M_at_66
#print axioms Delta4Gen.L8.slice_20
#print axioms Delta4Gen.L8.M_at_67
#print axioms Delta4Gen.L8.slice_21
#print axioms Delta4Gen.L8.M_at_83
#print axioms Delta4Gen.L8.slice_24
#print axioms Delta4Gen.L8.M_at_87
#print axioms Delta4Gen.L8.slice_26
#print axioms Delta4Gen.L8.M_at_94
#print axioms Delta4Gen.L8.slice_28
#print axioms Delta4Gen.L8.M_at_96
#print axioms Delta4Gen.L8.slice_30
#print axioms Delta4Gen.L8.M_at_107
#print axioms Delta4Gen.L8.slice_34
#print axioms Delta4Gen.L8.M_at_111
#print axioms Delta4Gen.L8.slice_36
#print axioms Delta4Gen.L8.M_at_114
#print axioms Delta4Gen.L8.slice_38
#print axioms Delta4Gen.L8.M_at_115
#print axioms Delta4Gen.L8.slice_39
#print axioms Delta4Gen.L8.M_at_118
#print axioms Delta4Gen.L8.slice_41
#print axioms Delta4Gen.L8.M_at_119
#print axioms Delta4Gen.L8.slice_42
#print axioms Delta4Gen.L8.M_at_122
#print axioms Delta4Gen.L8.slice_44
#print axioms Delta4Gen.L8.M_at_127
#print axioms Delta4Gen.L8.slice_47
#print axioms Delta4Gen.L8.M_at_143
#print axioms Delta4Gen.L8.slice_52
#print axioms Delta4Gen.L8.M_at_146
#print axioms Delta4Gen.L8.slice_54
#print axioms Delta4Gen.L8.M_at_147
#print axioms Delta4Gen.L8.slice_55
#print axioms Delta4Gen.L8.M_at_149
#print axioms Delta4Gen.L8.slice_57
#print axioms Delta4Gen.L8.M_at_153
#print axioms Delta4Gen.L8.slice_59
#print axioms Delta4Gen.L8.M_at_154
#print axioms Delta4Gen.L8.slice_60
#print axioms Delta4Gen.L8.M_at_159
#print axioms Delta4Gen.L8.slice_62
#print axioms Delta4Gen.L8.M_at_161
#print axioms Delta4Gen.L8.slice_64
#print axioms Delta4Gen.L8.M_at_165
#print axioms Delta4Gen.L8.slice_66
#print axioms Delta4Gen.L8.layer_count
#print axioms Delta4Gen.L8.layer_ok
